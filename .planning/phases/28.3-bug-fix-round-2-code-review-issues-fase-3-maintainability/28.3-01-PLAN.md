---
phase: 28.3-bug-fix-round-2-code-review-issues-fase-3-maintainability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/modules/saveContext.js
  - src/modules/searchIndexer.js
  - src/agents/utils/linkBuilder.js
  - src/utils/fileUtils.js
  - src/modules/intelligence.js
  - src/modules/contextValidator.js
autonomous: true
requirements: [MAINT-01]
must_haves:
  truths:
    - "CONTEXT_SESSION_DIR is not redefined in any of the 6 source files"
    - "All 6 files import CONTEXT_SESSION_DIR from src/config.js"
    - "Backward-compatible exports are preserved in linkBuilder.js and fileUtils.js"
    - "Test suite passes after changes"
  artifacts:
    - path: "src/modules/saveContext.js"
      provides: "Updated to import CONTEXT_SESSION_DIR from config.js"
      contains: "import.*CONTEXT_SESSION_DIR.*from.*config\\.js"
    - path: "src/modules/searchIndexer.js"
      provides: "Updated to import CONTEXT_SESSION_DIR from config.js"
      contains: "import.*CONTEXT_SESSION_DIR.*from.*config\\.js"
    - path: "src/agents/utils/linkBuilder.js"
      provides: "Updated to import CONTEXT_SESSION_DIR from config.js"
      contains: "import.*CONTEXT_SESSION_DIR.*from.*config\\.js"
    - path: "src/utils/fileUtils.js"
      provides: "Updated to import CONTEXT_SESSION_DIR from config.js"
      contains: "import.*CONTEXT_SESSION_DIR.*from.*config\\.js"
    - path: "src/modules/intelligence.js"
      provides: "Updated to import CONTEXT_SESSION_DIR from config.js"
      contains: "import.*CONTEXT_SESSION_DIR.*from.*config\\.js"
    - path: "src/modules/contextValidator.js"
      provides: "Updated to import CONTEXT_SESSION_DIR from config.js"
      contains: "import.*CONTEXT_SESSION_DIR.*from.*config\\.js"
  key_links:
    - from: "src/config.js"
      to: "src/modules/saveContext.js"
      pattern: "import.*CONTEXT_SESSION_DIR.*from.*'\\.\\./config\\.js'"
    - from: "src/config.js"
      to: "src/modules/searchIndexer.js"
      pattern: "import.*CONTEXT_SESSION_DIR.*from.*'\\.\\./config\\.js'"
    - from: "src/config.js"
      to: "src/agents/utils/linkBuilder.js"
      pattern: "import.*CONTEXT_SESSION_DIR.*from.*'\\.\\./\\.\\./config\\.js'"
    - from: "src/config.js"
      to: "src/utils/fileUtils.js"
      pattern: "import.*CONTEXT_SESSION_DIR.*from.*'\\.\\./config\\.js'"
    - from: "src/config.js"
      to: "src/modules/intelligence.js"
      pattern: "import.*CONTEXT_SESSION_DIR.*from.*'\\.\\./config\\.js'"
    - from: "src/config.js"
      to: "src/modules/contextValidator.js"
      pattern: "import.*CONTEXT_SESSION_DIR.*from.*'\\.\\./config\\.js'"
---

<objective>
Centralize CONTEXT_SESSION_DIR constant by replacing 6 redundant definitions with imports from src/config.js

Purpose: Eliminate duplication and drift risk — when CONTEXT_SESSION_DIR changes, only src/config.js needs updating.
Output: All 6 files import CONTEXT_SESSION_DIR from config.js; tests pass.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
From src/config.js (lines 5, 156):
```js
const CONTEXT_SESSION_DIR = '.opencode/context-session';
// ...
export { LOG_FILE, CONTEXT_SESSION_DIR };
```

Relative import paths per file:
- `src/modules/saveContext.js` → `'../config.js'` (already imports `getConfig` from there)
- `src/modules/searchIndexer.js` → `'../config.js'` (no existing import from config.js)
- `src/agents/utils/linkBuilder.js` → `'../../config.js'` (no existing import from config.js)
- `src/utils/fileUtils.js` → `'../config.js'` (no existing import from config.js)
- `src/modules/intelligence.js` → `'../config.js'` (already imports `getConfig` from there)
- `src/modules/contextValidator.js` → `'../config.js'` (no existing import from config.js)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update saveContext.js, searchIndexer.js, intelligence.js, contextValidator.js to import CONTEXT_SESSION_DIR from config.js</name>
  <files>
    src/modules/saveContext.js
    src/modules/searchIndexer.js
    src/modules/intelligence.js
    src/modules/contextValidator.js
  </files>
  <action>
    For each file, replace the local `const CONTEXT_SESSION_DIR = '.opencode/context-session';` declaration with an import from config.js.

    Specific changes:

    1. **src/modules/saveContext.js:22**
       - Line 22: `const CONTEXT_SESSION_DIR = '.opencode/context-session';`
       - Line 4 already has: `import { getConfig } from '../config.js';`
       - Change to: `import { getConfig, CONTEXT_SESSION_DIR } from '../config.js';`
       - Remove the local `const` declaration on line 22.

    2. **src/modules/searchIndexer.js:11**
       - Line 11: `const CONTEXT_SESSION_DIR = '.opencode/context-session';`
       - Add after existing imports (after line 9): `import { CONTEXT_SESSION_DIR } from '../config.js';`
       - Remove the local `const` declaration on line 11.
       - Note: The `const INDEX_DIR` on line 12 uses CONTEXT_SESSION_DIR via string concatenation, so it must remain as-is. Only replace the CONTEXT_SESSION_DIR definition.

    3. **src/modules/intelligence.js:10**
       - Line 10: `const CONTEXT_SESSION_DIR = '.opencode/context-session';`
       - Line 5 already has: `import { getConfig } from '../config.js';`
       - Change to: `import { getConfig, CONTEXT_SESSION_DIR } from '../config.js';`
       - Remove the local `const` declaration on line 10.

    4. **src/modules/contextValidator.js:7**
       - Line 7: `const CONTEXT_SESSION_DIR = '.opencode/context-session';`
       - Add after existing imports (after line 4): `import { CONTEXT_SESSION_DIR } from '../config.js';`
       - Remove the local `const` declaration on line 7.
  </action>
  <verify>
    <automated>node --experimental-vm-modules node_modules/jest/bin/jest.js --no-coverage 2>&1 | tail -20</automated>
  </verify>
  <done>All 4 files import CONTEXT_SESSION_DIR from config.js with no local redefinition; test suite passes.</done>
</task>

<task type="auto">
  <name>Task 2: Update linkBuilder.js and fileUtils.js to import from config.js while preserving backward-compatible exports</name>
  <files>
    src/agents/utils/linkBuilder.js
    src/utils/fileUtils.js
  </files>
  <action>
    For each file, replace the local definition with an import from config.js. Both files EXPORT CONTEXT_SESSION_DIR, so re-exports must be preserved.

    Specific changes:

    1. **src/agents/utils/linkBuilder.js:4**
       - Line 4: `export const CONTEXT_SESSION_DIR = '.opencode/context-session';`
       - Remove line 4. Add at top of file (after imports): `import { CONTEXT_SESSION_DIR } from '../../config.js';`
       - Add re-export: `export { CONTEXT_SESSION_DIR };` after the import.
       - This preserves backward compatibility since external code imports CONTEXT_SESSION_DIR from linkBuilder.

    2. **src/utils/fileUtils.js:10**
       - Line 10: `export const CONTEXT_SESSION_DIR = '.opencode/context-session';`
       - Remove line 10. Add at top of file (after imports): `import { CONTEXT_SESSION_DIR } from '../config.js';`
       - Add re-export: `export { CONTEXT_SESSION_DIR };` after the import.
       - This preserves backward compatibility since external code imports CONTEXT_SESSION_DIR from fileUtils.
  </action>
  <verify>
    <automated>node --experimental-vm-modules node_modules/jest/bin/jest.js --no-coverage 2>&1 | tail -20</automated>
  </verify>
  <done>Both files import CONTEXT_SESSION_DIR from config.js and re-export it; test suite passes.</done>
</task>

</tasks>

<verification>
Run full test suite: `npm test` — all existing tests pass without modification.
Quick grep to confirm no remaining local definitions: `rg "const CONTEXT_SESSION_DIR = '\.opencode/context-session'" src/`
</verification>

<success_criteria>
- [ ] grep for `const CONTEXT_SESSION_DIR = '.opencode/context-session'` in src/ returns zero matches
- [ ] Each of the 6 files has `import { CONTEXT_SESSION_DIR } from` (or `import { ..., CONTEXT_SESSION_DIR } from`)
- [ ] `npm test` passes with no failures
- [ ] linkBuilder.js still exports CONTEXT_SESSION_DIR (re-export)
- [ ] fileUtils.js still exports CONTEXT_SESSION_DIR (re-export)
</success_criteria>

<output>
After completion, create `.planning/phases/28.3-bug-fix-round-2-code-review-issues-fase-3-maintainability/28.3-01-SUMMARY.md`
</output>
