---
phase: 09-enhanced-summaries
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/agents/generateWeekly.js
  - tests/reportGenerator.test.js
autonomous: true
requirements: [SUMM-02]

must_haves:
  truths:
    - "Week summary shows Goals for the week from all daily sessions"
    - "Week summary shows Accomplishments for the week aggregated from days"
    - "Week summary shows Discoveries for the week from all days"
    - "Week summary shows Bugs Fixed with symptom/solution pairs"
    - "Week summary shows Relevant Files for the week"
  artifacts:
    - path: "src/agents/generateWeekly.js"
      provides: "Enhanced weekly summary with content aggregation"
      min_lines: 100
  key_links:
    - from: "src/agents/generateWeekly.js"
      to: "src/modules/contentExtractor.js"
      via: "uses extractSessionContent, extractBugs, findPatterns"
    - from: "src/agents/generateWeekly.js"
      to: "src/modules/reportGenerator.js"
      via: "adapts aggregateAccomplishments, aggregateBugs patterns"
---

<objective>
Enhance weekly summary generation to use contentExtractor.js for aggregating meaningful content from all days, not just file counts.

Purpose: Make week-summary.md a rich document showing what was accomplished across the week.
Output: Enhanced week-summary.md with Goals, Accomplishments, Discoveries, Bugs Fixed, Relevant Files aggregated from daily sessions.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/agents/generateWeekly.js
@src/modules/reportGenerator.js (for aggregation patterns)
@src/modules/contentExtractor.js (for extraction functions)
</context>

<interfaces>
<!-- Key types and contracts the executor needs. -->

From reportGenerator.js (aggregation patterns to adapt):
```javascript
function aggregateAccomplishments(sessions) {
  // Returns [{ text, date, source }] deduplicated by first 50 chars
}

function aggregateBugs(sessions) {
  // Returns [{ symptom, cause, solution, prevention, date, source }]
}

function extractDecisions(sessions) {
  // Returns [{ text, date, source }]
}
```

From generateWeekly.js (current structure):
```javascript
export async function generateWeeklySummary(directory, weekDate)
```

Current behavior:
- Reads day-summary.md files from each day directory
- Only extracts counts (compacts, exits per day)
- Builds simple list of day summaries with counts

Desired behavior:
- Read session files directly from each day directory
- Use contentExtractor to extract Goals, Accomplishments, Discoveries, Bugs
- Aggregate with deduplication
- Show per-day breakdown with content
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add contentExtractor imports and aggregation helpers</name>
  <files>src/agents/generateWeekly.js</files>
  <action>
Add imports at the top of generateWeekly.js:
- import extractSessionContent, extractBugs, findPatterns from '../../modules/contentExtractor.js'
- import fs from 'fs/promises' (if not already)

Add helper functions adapted from reportGenerator.js:

1. `aggregateWeekAccomplishments(allSessions)` - Aggregates accomplishments from all sessions, deduplicates by first 50 chars

2. `aggregateWeekBugs(allSessions)` - Aggregates bugs from all sessions

3. `formatWeeklyContent(year, weekStr, dailySessions)` - Formats the week summary:
   - Header with week number and period
   - ## Goals (aggregated from all sessions)
   - ## Accomplishments (aggregated with ✅ checkmarks)
   - ## Discoveries (aggregated)
   - ## Bugs Fixed (with symptom/solution)
   - ## Relevant Files (deduplicated, unique file paths)
   - ## Day-by-Day Summary (per-day breakdown with top accomplishments)

Use this format:
```markdown
## Week W17 Summary - 2026-04

### Goals
- Implement user authentication flow (Mon)
- Fix memory leak in cache (Wed)

### Accomplishments
- ✅ Added JWT middleware to API routes
- ✅ Resolved context eviction bug

### Discoveries
- Found that Redis connection pooling helps with performance

### Bugs Fixed
- Token limit exceeded: Added streaming (Tue)

### Relevant Files
- src/auth/jwt.js
- src/utils/cache.js
- src/middleware/auth.ts
```

Deduplication: Use first 50 chars as key for accomplishments.
</action>
  <verify>node -e "import('./src/agents/generateWeekly.js').then(m => console.log(typeof m.generateWeeklySummary))"</verify>
  <done>contentExtractor imported, aggregation helpers exist</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite generateWeeklySummary to read sessions and extract content</name>
  <files>src/agents/generateWeekly.js</files>
  <action>
Rewrite `generateWeeklySummary(directory, weekDate)` to:

1. Read ALL session files from each day directory (not just day-summary.md)
   - For each day (01-07), read all compact-*.md and exit-*.md files
   - Extract content using contentExtractor functions

2. Build comprehensive week summary:
   - Collect all goals, accomplishments, discoveries, bugs, files
   - Deduplicate similar entries
   - Find patterns across sessions using findPatterns()

3. Format with structured sections (see format above)

4. Write to week-summary.md in the week directory

Keep existing functionality:
- Week number calculation with date-fns getWeek()
- Path structure: .opencode/context-session/YYYY/MM/WW/week-summary.md
- Header with frontmatter for Obsidian

Key change: Instead of reading day-summary.md files and aggregating counts, read raw session files and aggregate content.
</action>
  <verify>ls .opencode/context-session/2026/04/W17/week-summary.md 2>/dev/null && head -50 .opencode/context-session/2026/04/W17/week-summary.md || echo "File not yet generated"</verify>
  <done>generateWeeklySummary produces content-rich week-summary.md</done>
</task>

</tasks>

<verification>
- generateWeeklySummary can be called without errors
- week-summary.md contains Goals, Accomplishments, Discoveries, Bugs Fixed, Relevant Files
- Deduplication works - no repeated entries
</verification>

<success_criteria>
1. Week summary shows aggregated Goals from all sessions in the week
2. Week summary shows aggregated Accomplishments from all days
3. Week summary shows Discoveries from sessions
4. Week summary shows Bugs Fixed with symptom/solution
5. Week summary shows Relevant Files aggregated
6. Day-by-day breakdown shows top accomplishment per day
</success_criteria>

<output>
After completion, create `.planning/phases/09-enhanced-summaries/09-02-SUMMARY.md`
</output>