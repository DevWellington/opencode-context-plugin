---
phase: 09-enhanced-summaries
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/modules/summaries.js
  - tests/summaries.test.js
autonomous: true
requirements: [SUMM-01]

must_haves:
  truths:
    - "Day summary contains Goals section with extracted goals from sessions"
    - "Day summary contains Accomplishments section with extracted accomplishments"
    - "Day summary contains Discoveries section when sessions have discoveries"
    - "Day summary contains Bugs Fixed section with bug details from sessions"
    - "Day summary contains Relevant Files section with file paths"
  artifacts:
    - path: "src/modules/summaries.js"
      provides: "Enhanced day summary with content extraction"
      min_lines: 150
    - path: "tests/summaries.test.js"
      provides: "Tests for new content extraction in day summary"
      min_lines: 50
  key_links:
    - from: "src/modules/summaries.js"
      to: "src/modules/contentExtractor.js"
      via: "import and use extractSessionContent, extractBugs"
    - from: "src/modules/summaries.js"
      to: ".opencode/context-session/YYYY/MM/WW/DD/"
      via: "reads session files, writes day-summary.md"
---

<objective>
Enhance day summary generation in summaries.js to use contentExtractor.js for extracting meaningful content (Goals, Accomplishments, Discoveries, Bugs, Relevant Files) instead of just file counts and references.

Purpose: Make day-summary.md a rich, useful document that captures what was worked on, not just that a session occurred.
Output: Enhanced day-summary.md with structured sections and content extraction.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/modules/summaries.js
@src/modules/contentExtractor.js
@src/agents/generateIntelligenceLearning.js (for pattern of using contentExtractor)
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From contentExtractor.js:
```javascript
export function extractSessionContent(sessionContent) {
  // Returns { goal, accomplished, discoveries, relevantFiles, firstUserMessage, raw, relatedSessions }
}

export function extractBugs(sessionContent) {
  // Returns [{ symptom, cause, solution, prevention }]
}
```

From summaries.js (current structure):
```javascript
export async function updateDaySummary(dirPath, sessionInfo)
export const updateDailySummary = debounce(updateDailySummaryImpl, debounceMs)
export const updateWeekSummary = debounce(updateWeekSummaryImpl, debounceMs)
```

Current updateDaySummary behavior:
- Takes dirPath and sessionInfo
- Appends entry to day-summary.md with filename and timestamp
- Only tracks that session occurred, not what was done
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add contentExtractor imports and helper functions</name>
  <files>src/modules/summaries.js</files>
  <action>
Import extractSessionContent and extractBugs from contentExtractor.js at the top of summaries.js.

Add two new helper functions:

1. `readDaySessions(dirPath)` - Reads all session files (exit-*.md, compact-*.md) from the directory and returns array of { filename, content, extracted, bugs }

2. `formatDayContent(dateStr, sessionsData)` - Formats the day summary with structured sections:
   - ## [Date]
   - ### Goals (from sessions that have goals)
   - ### Accomplishments (from sessions that have accomplished)
   - ### Discoveries (from sessions that have discoveries)
   - ### Bugs Fixed (from sessions that have bugs with solutions)
   - ### Relevant Files (aggregated from all sessions)

Use the format shown in the user's example:
```markdown
## 2026-04-22

### Goals
- Implement user authentication flow

### Accomplishments
- ✅ Added JWT middleware to API routes

### Bugs Fixed
- Token limit exceeded: Added streaming with chunked injection

### Relevant Files
- src/auth/jwt.js (new)
```

Deduplication: Use first 50 chars as key (like reportGenerator.js does) to avoid duplicate entries.
</action>
  <verify>node -e "import('./src/modules/summaries.js').then(m => console.log(typeof m.updateDaySummary))"</verify>
  <done>contentExtractor imported, helper functions exist</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite updateDaySummary to use content extraction</name>
  <files>src/modules/summaries.js</files>
  <action>
Rewrite the `updateDaySummary(dirPath, sessionInfo)` function to:

1. Read ALL session files from dirPath (not just append current session)
2. For each session file, use extractSessionContent() and extractBugs()
3. Build a structured day summary with sections (Goals, Accomplishments, Discoveries, Bugs Fixed, Relevant Files)
4. Write the complete day-summary.md with all extracted content

The function signature stays the same (dirPath, sessionInfo) but behavior changes:
- Instead of just appending a reference, it regenerates the FULL day summary from all sessions
- This ensures the day summary is always complete and contains extracted content

Key changes:
- Read all .md files from dirPath (exit-*.md, compact-*.md)
- Skip day-summary.md itself if it exists
- Extract content from each session using contentExtractor
- Aggregate into sections (deduplicated)
- Write complete formatted content

Keep idempotency: Only write if content changed (compare with existing).
</action>
  <verify>npm test -- --filter=summaries.test.js 2>&1 | tail -30</verify>
  <done>updateDaySummary generates content-rich day-summary.md</done>
</task>

<task type="auto">
  <name>Task 3: Update tests for new behavior</name>
  <files>tests/summaries.test.js</files>
  <action>
Update tests/summaries.test.js to test the new content extraction behavior:

1. Test that updateDaySummary extracts goals from session content
2. Test that updateDaySummary extracts accomplishments
3. Test that updateDaySummary extracts bugs with solutions
4. Test that updateDaySummary deduplicates similar entries
5. Test that Relevant Files section contains file paths

Create test session files with structured content like:
```markdown
## Goal
Implement authentication flow

## Accomplished
- Added JWT middleware

### Bug: Token limit exceeded
**Solution:** Added streaming with chunked injection
```

Verify the output day-summary.md contains these extracted sections.
</action>
  <verify>npm test -- --filter=summaries.test.js -x</verify>
  <done>All tests pass with new content extraction behavior</done>
</task>

</tasks>

<verification>
- npm test -- --filter=summaries.test.js passes
- day-summary.md contains Goals, Accomplishments, Discoveries, Bugs Fixed, Relevant Files sections
- No duplicate entries in sections
</verification>

<success_criteria>
1. updateDaySummary extracts and displays Goals from sessions
2. updateDaySummary extracts and displays Accomplishments from sessions
3. updateDaySummary extracts and displays Bugs with solutions from sessions
4. updateDaySummary aggregates Relevant Files from all sessions
5. Deduplication works (no repeat entries)
6. Tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/09-enhanced-summaries/09-01-SUMMARY.md`
</output>