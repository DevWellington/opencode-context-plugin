---
phase: "07.3"
plan: "02"
type: "execute"
wave: 2
depends_on: ["07.3-01"]
files_modified:
  - "src/agents/generateToday.js"
  - "src/agents/generateWeekly.js"
  - "src/agents/generateMonthly.js"
  - "src/agents/generateAnnual.js"
autonomous: true
requirements:
  - "SMART-02"
must_haves:
  truths:
    - "generateToday skips regeneration when change < 5% and no new sessions"
    - "generateWeekly skips regeneration when change < 5%"
    - "generateMonthly skips regeneration when change < 5%"
    - "generateAnnual skips regeneration when change < 5%"
  artifacts:
    - path: "src/agents/generateToday.js"
      provides: "ShouldRegenerate integration with shouldRegenerate()"
    - path: "src/agents/generateWeekly.js"
      provides: "ShouldRegenerate integration with shouldRegenerate()"
    - path: "src/agents/generateMonthly.js"
      provides: "ShouldRegenerate integration with shouldRegenerate()"
    - path: "src/agents/generateAnnual.js"
      provides: "ShouldRegenerate integration with shouldRegenerate()"
---

<objective>
Integrate shouldRegenerate() into each generate agent to skip unnecessary regeneration.

Purpose: Only regenerate summaries when content exceeds threshold or meaningful changes occurred.
Output: Each generate agent checks shouldRegenerate before writing.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/agents/generateToday.js (existing - will add shouldRegenerate check)
@src/agents/generateWeekly.js (existing - will add shouldRegenerate check)
@src/agents/generateMonthly.js (existing - will add shouldRegenerate check)
@src/agents/generateAnnual.js (existing - will add shouldRegenerate check)
@.planning/phases/07.3-smart-generation-triggers/07.3-01-PLAN.md (07.3-01 creates shouldRegenerate)

From 07.3-01:
- shouldRegenerate(oldContent, newContent, threshold) returns { shouldRegenerate, savingsPercent, changePercent }
- hasNewSessions(existingSummary, newSessions) returns boolean
- Both exported from src/modules/summaries.js

Success criteria:
1. Skips regeneration when change < 5%
2. Only regenerates if new session added
3. Maintains backward compatibility
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add shouldRegenerate check to generateToday.js</name>
  <files>src/agents/generateToday.js</files>
  <action>
    Modify generateToday.js to add shouldRegenerate check:

    1. Import shouldRegenerate from '../modules/summaries.js'
       ```javascript
       import { shouldRegenerate } from '../modules/summaries.js';
       ```

    2. After building the new body content, before writing, read existing file and check:
       ```javascript
       // Check if regeneration is needed
       let existingContent = '';
       try {
         existingContent = await fs.readFile(savePath, 'utf-8');
       } catch {
         // File doesn't exist, need to regenerate
       }
       
       const { shouldRegenerate: needsRegen, changePercent } = shouldRegenerate(existingContent, fullReport);
       
       if (!needsRegen) {
         logger('[generateToday] Skipped - no meaningful change (change: ' + changePercent + '%)');
         return existingContent;
       }
       ```

    3. Keep the rest of the logic the same - this is a conditional early return.
  </action>
  <verify>
    <automated>npm test -- --filter=summaries 2>&1 | tail -15</automated>
  </verify>
  <done>generateToday skips regeneration when change < 5%</done>
</task>

<task type="auto">
  <name>Task 2: Add shouldRegenerate check to generateWeekly.js</name>
  <files>src/agents/generateWeekly.js</files>
  <action>
    Modify generateWeekly.js to add shouldRegenerate check:

    1. Import shouldRegenerate from '../modules/summaries.js'
       ```javascript
       import { shouldRegenerate } from '../modules/summaries.js';
       ```

    2. After building the fullReport content (after budget truncation), before writing:
       ```javascript
       // Check if regeneration is needed
       let existingContent = '';
       try {
         existingContent = await fs.readFile(savePath, 'utf-8');
       } catch {
         // File doesn't exist
       }
       
       const { shouldRegenerate: needsRegen, changePercent } = shouldRegenerate(existingContent, fullReport);
       
       if (!needsRegen) {
         logger('[generateWeekly] Skipped - no meaningful change (change: ' + changePercent + '%)');
         return existingContent;
       }
       ```

    3. Place this check right before the fs.writeFile call.
  </action>
  <verify>
    <automated>npm test -- --filter=summaries 2>&1 | tail -15</automated>
  </verify>
  <done>generateWeekly skips regeneration when change < 5%</done>
</task>

<task type="auto">
  <name>Task 3: Add shouldRegenerate check to generateMonthly.js</name>
  <files>src/agents/generateMonthly.js</files>
  <action>
    Modify generateMonthly.js to add shouldRegenerate check:

    1. Import shouldRegenerate from '../modules/summaries.js'
       ```javascript
       import { shouldRegenerate } from '../modules/summaries.js';
       ```

    2. After building the fullReport content (after budget truncation), before writing:
       ```javascript
       // Check if regeneration is needed
       let existingContent = '';
       try {
         existingContent = await fs.readFile(savePath, 'utf-8');
       } catch {
         // File doesn't exist
       }
       
       const { shouldRegenerate: needsRegen, changePercent } = shouldRegenerate(existingContent, fullReport);
       
       if (!needsRegen) {
         logger('[generateMonthly] Skipped - no meaningful change (change: ' + changePercent + '%)');
         return existingContent;
       }
       ```

    3. Place this check right before the fs.writeFile call.
  </action>
  <verify>
    <automated>npm test -- --filter=summaries 2>&1 | tail -15</automated>
  </verify>
  <done>generateMonthly skips regeneration when change < 5%</done>
</task>

<task type="auto">
  <name>Task 4: Add shouldRegenerate check to generateAnnual.js</name>
  <files>src/agents/generateAnnual.js</files>
  <action>
    Modify generateAnnual.js to add shouldRegenerate check:

    1. Import shouldRegenerate from '../modules/summaries.js'
       ```javascript
       import { shouldRegenerate } from '../modules/summaries.js';
       ```

    2. After building the fullReport content (after budget truncation), before writing:
       ```javascript
       // Check if regeneration is needed
       let existingContent = '';
       try {
         existingContent = await fs.readFile(savePath, 'utf-8');
       } catch {
         // File doesn't exist
       }
       
       const { shouldRegenerate: needsRegen, changePercent } = shouldRegenerate(existingContent, fullReport);
       
       if (!needsRegen) {
         logger('[generateAnnual] Skipped - no meaningful change (change: ' + changePercent + '%)');
         return existingContent;
       }
       ```

    3. Place this check right before the fs.writeFile call.
  </action>
  <verify>
    <automated>npm test -- --filter=summaries 2>&1 | tail -15</automated>
  </verify>
  <done>generateAnnual skips regeneration when change < 5%</done>
</task>

</tasks>

<verification>
- All generate agents skip regeneration when change < 5%
- Existing content is returned when skipped
- New/changed content is written when change >= 5%
- Backward compatibility maintained - no change to output format
</verification>

<success_criteria>
1. generateToday skips when change < 5%
2. generateWeekly skips when change < 5%
3. generateMonthly skips when change < 5%
4. generateAnnual skips when change < 5%
5. All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/07.3-smart-generation-triggers/07.3-02-SUMMARY.md`
</output>
